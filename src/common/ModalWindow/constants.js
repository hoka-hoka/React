const lang = ['Cancel', 'Apply'];
const langData = {
  cancel: 0,
  apply: 1,
};
const interElems = ['A', 'BUTTON', 'INPUT'];
const focusArea = {
  start: 'start',
  end: 'end',
};

export { lang, langData, interElems, focusArea };
